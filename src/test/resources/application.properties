# Test database (H2 in-memory)
db.url=jdbc:h2:mem:otpdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
db.username=sa
db.password=
db.pool.size=5

# JWT
jwt.secret=testSecretKeyForJwtThatIsAtLeast32BytesLong!!!
jwt.expiration.seconds=3600

# Server (??? ?????????????? ??????)
server.port=18080
